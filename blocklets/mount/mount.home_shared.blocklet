#!/bin/bash

mountStat=$(mount | grep /home/emeraldinspirations/Shared);

if [ "$mountStat" '==' "/dev/mapper/linuxvg-home_shared on /home/emeraldinspirations/Shared type btrfs (rw,relatime,space_cache,subvolid=5,subvol=/)" ]; then
  echo "  "
else
  echo "<span color='#D80022'>  </span>"
fi
