sudo echo "deb http://debian.sur5r.net/i3/ $(lsb_release -c -s) universe" >> /etc/apt/sources.list
sudo apt-get update
sudo apt-get --allow-unauthenticated install sur5r-keyring
sudo apt-get update
sudo apt-get install i3

# Install font-awesome

# Prep for settings
mkdir -p ~/.i3

# Add Symbolic Links
ln -s ~/Install/i3/.i3status.conf ~/.i3status.conf
ln -s ~/Install/i3/config_ ~/.i3/config

# Note: config_ to be renamed when symbolic links on old partition re-directed

# Install Desktop Background
sudo apt install feh

# Install Lock
sudo apt-get install i3lock scrot imagemagick xautolock
